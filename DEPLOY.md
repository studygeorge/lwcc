# LWCC - Lakewood Construction Company

Сайт строительной компании Lakewood Construction.

## 🚀 Деплой на сервер

### Шаг 1: Подключение к серверу

```bash
ssh root@cv5534303
```

### Шаг 2: Переход в директорию проекта

```bash
cd /home/lwcc
```

### Шаг 3: Получение последних изменений из Git

```bash
git pull origin main
```

### Шаг 4: Установка зависимостей (если нужно)

```bash
# Для фронтенда
cd front
npm install

# Для бота
cd ../bottg
npm install
```

### Шаг 5: Сборка фронтенда для production

```bash
cd /home/lwcc/front
npm run build
```

Это создаст папку `build/` с готовыми файлами для production.

### Шаг 6: Настройка Nginx (если используется)

Конфигурация Nginx для раздачи статики:

```nginx
server {
    listen 80;
    server_name lwcc.ru www.lwcc.ru;

    root /home/lwcc/front/build;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

Перезапуск Nginx:
```bash
nginx -t
systemctl restart nginx
```

### Шаг 7: Запуск Telegram бота

```bash
cd /home/lwcc/bottg

# Через PM2 (рекомендуется)
npm run pm2:start

# Или напрямую (для теста)
npm start
```

Проверка статуса бота:
```bash
npm run pm2:status
```

Просмотр логов:
```bash
npm run pm2:logs
```

---

## 🛠️ Разработка локально

### Запуск dev сервера

```bash
cd front
npm install
npm run dev
```

Сайт будет доступен на http://localhost:5173

### Запуск бота

```bash
cd bottg
npm install
npm start
```

---

## 📂 Структура проекта

```
lwcc/
├── front/          # React фронтенд (Vite)
│   ├── src/        # Исходники React
│   ├── public/     # Статические файлы
│   ├── build/      # Production сборка (создается после npm run build)
│   └── package.json
├── front-v2/       # Резервная копия (чистая версия)
├── bottg/          # Telegram бот
│   ├── telegram-bot.js
│   ├── ecosystem.config.js  # PM2 конфиг
│   └── package.json
└── README.md
```

---

## 🔧 Полезные команды

### Фронтенд

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run preview  # Просмотр production сборки
```

### Telegram бот

```bash
npm start              # Запуск бота напрямую
npm run pm2:start      # Запуск через PM2
npm run pm2:stop       # Остановка
npm run pm2:restart    # Перезапуск
npm run pm2:logs       # Просмотр логов
npm run pm2:status     # Статус
npm run pm2:delete     # Удаление из PM2
```

---

## 🔄 Быстрый деплой (одна команда)

Создайте алиас в `~/.bashrc` на сервере:

```bash
# Добавьте в ~/.bashrc
alias lwcc-deploy='cd /home/lwcc && git pull origin main && cd front && npm install && npm run build && echo "✅ Деплой завершен!"'
```

Затем просто выполняйте:

```bash
lwcc-deploy
```

---

## 🐛 Проблемы и решения

### Проблема: Скролл не работает

**Решение:** Убедитесь, что используется `front/` (не `front-v2/`), в нем уже исправлен скролл.

### Проблема: Бот не получает сообщения

**Решение:** 
1. Проверьте токен в `bottg/telegram-bot.js`
2. Проверьте ADMIN_CHAT_ID
3. Проверьте статус: `npm run pm2:status`
4. Посмотрите логи: `npm run pm2:logs`

### Проблема: Nginx выдает 404

**Решение:**
1. Проверьте путь к `build/`: `ls -la /home/lwcc/front/build/`
2. Проверьте конфиг Nginx: `nginx -t`
3. Проверьте права: `chmod -R 755 /home/lwcc/front/build/`

---

## 📞 Контакты

Токен бота: `8308614851:AAHJCjqO_iQyTbLyFlNeHXy73ksvmU6xOtg`
Admin Chat ID: `5031906741`

---

## 📝 История версий

- **v2.0** (front-v2) - Чистая версия с исправленным скроллом
- **v1.0** (front) - Рабочая версия (скопировано из front-v2)
